---
const user = Astro.locals.user;

if (user) {
  return new Response(null, {
    status: 302,
    headers: {
      Location: "/admin/users/dashboardUsers"
    }
  });
} else {
  return new Response(null, {
    status: 302,
    headers: {
      Location: "/login"
    }
  });
}
---
